/* Не используя дополнительных переменных, поменять местами значения
двух переменных типа int */

#include <stdio.h>

int main()
{
    int x, y;
    x = 15;
    y = 23;
    x = x + y;
    y = x - y;
    x = x - y;
    printf("\n%d\n%d", x, y);
}
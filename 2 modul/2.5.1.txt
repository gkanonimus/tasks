/* Целой переменной k присвоить значение, равное третьей от конца цифре в
записи целого положительного числа x */
#include <stdio.h>
int main()
{
    int k, x;
    x = 56789;
    k = (x / 100) % 10;
    printf("\n%d\n", k);
}